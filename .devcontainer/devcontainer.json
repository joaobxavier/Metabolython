{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "postCreateCommand": "wget -O miniconda.sh https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh && bash miniconda.sh -b -p /opt/conda && rm miniconda.sh && echo 'export PATH=/opt/conda/bin:$PATH' >> ~/.bashrc && /opt/conda/bin/conda install -c conda-forge rdkit jupyter -y && /opt/conda/bin/pip install bioservices pubchempy",
  "forwardPorts": [8888],
  "remoteUser": "vscode"
}
